# Keep repository text files identical on Windows, Linux, and macOS.
* text=auto eol=lf

# Windows-native command files retain their expected line endings.
*.bat text eol=crlf
*.cmd text eol=crlf
*.ps1 text eol=crlf

# Do not attempt line-ending conversion on binary assets.
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.pdf binary
*.woff binary
*.woff2 binary

# The final tab in this GERP export represents its required empty last column.
automation/fixtures/demo-history/Other_Account_Transaction_Text_synthetic_* whitespace=-trailing-space
